<UserControl x:Class="LoginWindow.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="5,0,5,0">
            <Label Content="Login"/>
            <TextBox Name="LoginTextBox"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="5,0,5,0">
            <Label Content="Password"/>
            <TextBox Name="PasswordTextBox"/>
        </StackPanel>
        <Grid Grid.Row="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Content="Log in" Grid.Column="0" Height="20" Width="50" Click="LogInClicked"/>
            <Button Content="Sign up" Grid.Column="1" Height="20" Width="50" Click="SignUpClicked"/>
        </Grid>
    </Grid>
</UserControl>
